{
  "name": "react-trpc-chat-app",
  "private": true,
  "version": "10.32.0",
  "root": true,
  "scripts": {
    "dev": "lerna run dev --stream"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "lerna": "^7.1.1"
  }
}