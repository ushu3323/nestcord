{
  "name": "react-trpc-chat-app",
  "private": true,
  "version": "10.32.0",
  "root": true,
  "scripts": {
    "dev:client": "npm run dev -w client -- --clearScreen=false",
    "dev:server": "npm run dev -w server",
    "dev": "run-p dev:* -lr"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}